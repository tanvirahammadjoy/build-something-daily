# TaskFlow — Client

This folder will hold the React (Vite) frontend.

It's scaffolded in **Phase 3**, once the core task API exists for it to talk to.
Planned stack: React + Vite, Tailwind CSS + shadcn/ui, React Query, Zustand,
Socket.io-client, FullCalendar.
